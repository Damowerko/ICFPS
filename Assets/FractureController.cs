using UnityEngine;
using UnityEngine.Networking;

public class FractureController : NetworkBehaviour {

	// Use this for initialization
	void Start () {
        //FracturedChunk[] chunks = gameObject.GetComponentsInChildren<FracturedChunk>();
        //foreach (FracturedChunk chunk in chunks)
        //{
        //    chunk.gameObject.AddComponent<NetworkIdentity>();
        //}
	}
	
}
